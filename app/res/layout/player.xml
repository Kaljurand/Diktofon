<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:padding="@dimen/paddingPlayer">

    <Button
        android:id="@+id/button_trans_playpause"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true"
        android:background="@drawable/button_play" />

    <TextView
        android:id="@+id/tv_trans_timer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_marginStart="@dimen/layoutMargin1"
        android:layout_marginEnd="@dimen/layoutMargin1"
        android:layout_toEndOf="@+id/button_trans_playpause"
        android:includeFontPadding="false"
        android:textAppearance="?android:attr/textAppearanceMedium"
        android:textColor="?android:textColorSecondary"
        tools:text="0:00" />

    <SeekBar
        android:id="@+id/seekbar_trans_seek"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_toEndOf="@+id/tv_trans_timer"
        android:max="100"
        android:progress="0" />

</RelativeLayout>